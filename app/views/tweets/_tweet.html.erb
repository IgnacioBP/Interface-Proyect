<!-- <div class="card mb-3">
  <div class="card-header d-flex justify-content-between align-items-center">
    <span><%= tweet.user.full_name %></span>
    <small class="text-muted"><%= tweet.created_at.strftime("%d %b %Y, %H:%M") %></small>
  </div>
  <div class="card-body">
    <p class="card-text"><%= tweet.body %></p>
    <% if tweet.hashtags.any? %>
      <p class="card-text">
        <% tweet.hashtags.each do |hashtag| %>
          <span class="badge bg-secondary">#<%= hashtag.name %></span>
        <% end %>
      </p>
    <% end %>
    <%= link_to 'Ver detalles', tweet_path(tweet), class: 'btn btn-primary btn-sm' %>
    <div class="mt-2">
      <% ReactionLevel.ordered_by_level.each do |reaction| %>
        <%= button_to tweet_react_path(tweet, reaction_level_id: reaction.id), method: :post, class: "btn btn-outline-secondary btn-sm me-1" do %>
          <%= reaction.emoji %> <%= reaction.name %>
        <% end %>
      <% end %>
    </div>
  </div>
  <div class="card-footer text-muted">
    <small>Reacciones: <%= tweet.user_tweet_reactions.count %></small>
  </div>
</div>
