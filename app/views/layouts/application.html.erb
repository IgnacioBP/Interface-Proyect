<!DOCTYPE html>
<html>
<head>
  <title>SimpleTwitter</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  
  <link href="https://fonts.googleapis.com/css2?family=Open+Sans:wght@400;600&display=swap" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-whatever-hash" crossorigin="anonymous">

  <!-- Bootstrap Icons -->
  <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-icons/1.9.1/font/bootstrap-icons.min.css" rel="stylesheet">
</head>
<body style="background-color: #f7f6dc;">

  <div class="container-fluid">
    <div class="row">
      <!-- Sidebar Column -->
      <% if user_signed_in? %>
 
      <div class="wrapper">
        <div class="col-12 col-sm-2 col-xl-2 px-sm-2 px-0 bg-dark d-flex flex-column sticky-top vh-100" id="mainSidebar">
          <%= render partial: 'layouts/navbar' %>
        </div>
      </div>

      <% end %>
      <!-- Main Content Column -->
      <main class="col-12 col-sm-9 col-xl-10 px-md-4">
        <%= render partial: 'layouts/flash' %>
        <%= yield %>
      </main>
      
    </div> 
  </div>

</body>
</html>

<style>
.wrapper {
  display: flex;
  width: 100%;
  align-items: stretch;
}

#mainSidebar {
  width: 150px; /* Ancho deseado del sidebar */
}

main {
  flex: 1; /* Toma el espacio restante */
  padding: 20px;
  min-height: 100vh; /* Asegúrate de que el contenido principal tenga suficiente altura */
}

</style>