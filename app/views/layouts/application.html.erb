<!DOCTYPE html>
<html>
<head>
  <title>🦆Quacker</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  
  <link href="https://fonts.googleapis.com/css2?family=Open+Sans:wght@400;600&display=swap" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-whatever-hash" crossorigin="anonymous">
  <link
    rel="stylesheet"
    href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css"
  />

  <!-- Bootstrap Icons -->
  <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-icons/1.9.1/font/bootstrap-icons.min.css" rel="stylesheet">
</head>
<body style="background-color: #f7f6dc;">

  <div class="container-fluid">
    <div class="row">
      <!-- Sidebar Column -->
      <% if user_signed_in? %>
 

      <div class=" wrapper col-12 col-sm-2 col-xl-2 px-sm-2 px-0 bg-dark d-flex flex-column sticky-top vh-100" id="mainSidebar">
        <%= render partial: 'layouts/navbar' %>
      </div>


      <% end %>
      <!-- Main Content Column -->
      <main class="col-12 col-sm-9 col-xl-10 px-md-4">
        <%= render partial: 'layouts/flash' %>
        <%= yield %>
      </main>
      
    </div> 
  </div>

  <% unless current_page?(new_user_session_path) || current_page?(new_user_registration_path) %>
    <%= link_to new_tweet_path, class: "animate__animated animate__jello floating-btn" do %>
      <%= image_tag("QuaDiagonal.png", alt: "New Tweet", class: "floating-btn-image") %>
    <% end %>
  <% end %>


</body>
</html>

<style>
.wrapper {
  display: flex;
  width: 100%;
  align-items: stretch;
}

#mainSidebar {
  width: 150px; /* Ancho deseado del sidebar */
}

main {
  flex: 1; /* Toma el espacio restante */
  padding: 20px;
  min-height: 100vh; /* Asegúrate de que el contenido principal tenga suficiente altura */
}

.floating-btn {
  position: fixed;
  bottom: 20px;
  right: 20px;
  background-color: #83cb35; /* Color de fondo del botón */
  padding: 15px;
  border-radius: 50px;
  transition: background-color 0.3s ease;
  text-decoration: none;
  z-index: 1000; /* Asegúrate de que el botón esté por encima de otros elementos */
  width: 100px;
  height: 100px;
}

.floating-btn:hover {
  background-color: #548d24; /* Color del botón al hacer hover */
}

.floating-btn-image {
  width: 100%; /* Ajusta el tamaño de la imagen */
  height: 100%;
  vertical-align: middle;
}


</style>